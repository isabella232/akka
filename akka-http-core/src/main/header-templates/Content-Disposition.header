name: Content-Disposition
spec: http://tools.ietf.org/html/rfc6266
rendering: { r ~~ dispositionType; params foreach { case (k, v) ⇒ r ~~ "; " ~~ k ~~ '=' ~~# v }; r }
parameters:
  dispositionType: headers.ContentDispositionType
  params: Map[String, String] = Map.empty
